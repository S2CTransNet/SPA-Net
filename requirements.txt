# Python 3.10. Used by install.sh after CUDA PyTorch is in place.
# timm is installed separately with --no-deps so it cannot replace the GPU torch.

h5py==3.12.1
numpy==1.26.4
open3d==0.18.0
plotly==5.24.1
setuptools>=77.0.3
tqdm==4.66.5
transforms3d==0.4.2
tensorboard==2.18.0
ninja==1.11.1.3
wheel==0.45.1
huggingface_hub
safetensors
pyyaml
